/*Од тастатура се внесуваат димензиите на една матрица (m,n <= 100), а потоа и елементите од матрицата. Да се трансформира матрицата така што средниот
елемент во секоја редица ќе се замени со разликата (по апсолутна вредност) на сумата на елементите во првата половина од редицата и сумата на елементите
во втората половина на редицата. Ако матрицата има парен број колони, се менува вредноста на средните два елемента. Средниот/те елемент/и влегува/ат во
сумите (при непарен број на колони, средниот елемент влегува во двете суми!). Да се испечати на екран променетата матрица.*/



#include <iostream>
#include <cmath>
using namespace std;
int main(){
    int m,n,i,j,mat[100][100];
    cin>>m>>n;
    for(i=0;i<m;i++){
        for(j=0;j<n;j++){
            cin>>mat[i][j];
        }
    }
    if(n%2==0){
        for(i=0;i<m;i++){
            int sumL=0, sumR=0;
            for(j=0;j<n/2;j++){
                sumL=sumL+mat[i][j];
            }
            for(j=n/2;j<n;j++){
                sumR=sumR+mat[i][j];
            }
            int aps=fabs(sumL-sumR);
            mat[i][n/2]=aps;
            mat[i][n/2-1]=aps;
        }
    }else{
        for(i=0;i<m;i++){
            int sumL=0, sumR=0;
            for(j=0;j<n/2+1;j++){
                sumL=sumL+mat[i][j];
            }
            for(j=n/2;j<n;j++){
                sumR=sumR+mat[i][j];
            }
            int aps=fabs(sumL-sumR);
            mat[i][n/2]=aps;
        }
    }
    for(i=0;i<m;i++){
        for(j=0;j<n;j++){
            cout<<mat[i][j]<<" ";
        }
        cout<<endl;
    }
}
